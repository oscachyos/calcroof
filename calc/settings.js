window.CALC_SETTINGS = {
    // Единый процент бригадира в расчетах выплат (calc19).
    foremanPercent: 15,

    // Процент запаса для расчета количества рулонов (calc18).
    rollReservePercent: 15
};
